# Copy to .env, fill in your key, and use uv run --env-file .env ...
FAL_KEY=

# Optional for Codex API authentication; otherwise use the runner's --login.
CODEX_API_KEY=

# Pi uses ChatGPT subscription auth via --agent pi --login; no API key is needed.
