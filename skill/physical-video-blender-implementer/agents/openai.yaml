interface:
  display_name: "Physical Video Blender Implementer"
  short_description: "Implement physical plans as white-clay Blender previews"
  default_prompt: "Use $physical-video-blender-implementer to turn this physical state plan into a standalone Blender script, render a white/clay preview using the requested profile, and extract a sheet preserving semantic keyframes and transitions."
